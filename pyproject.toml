[project]
name = "artificial-neural-networks-from-scratch-dr-fridolin"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.2",
    "prek>=0.4.13",
]

[tool.uv]
exclude-newer = "30 days"
